# CI/CD 作業報告

- 學號：313554006
- 姓名：（請填入）
- Repo：https://github.com/a36579066/cicd-lab

---

## 一、CI Pipeline 說明

### 1. Workflow 檔案

檔案路徑：`.github/workflows/ci_313554006.yaml`

```yaml
name: CI (313554006)

on:
  push:
    branches:
      - '**'
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-313554006-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Typecheck / Prettier / Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: TypeScript typecheck
        run: npm run typecheck

      - name: Prettier check
        run: npm run format:check

      - name: Run tests (JUnit report)
        run: |
          mkdir -p reports
          npm test -- --reporter=default --reporter=github-actions --reporter=junit --outputFile.junit=reports/vitest-junit.xml

      - name: Generate test summary
        if: always()
        run: node scripts/junit-summary.mjs reports/vitest-junit.xml

      - name: Upload test report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vitest-junit-report
          path: reports/
          if-no-files-found: error
```

### 2. Pipeline 設計說明

本 pipeline 設計目標是讓每一次 `push` 都能自動驗證程式碼的「型別正確性、格式一致性、測試通過」，並把測試結果直接呈現在 GitHub Actions 頁面。設計上刻意**只使用 GitHub 官方 action**，測試結果呈現改以 vitest 內建 reporter 搭配自寫 script 完成，不依賴任何第三方 action。

**(1) 觸發時機（push 時自動執行）**

- `on.push.branches: ['**']`：任何 branch 的 push 都會觸發，符合「push 時自動執行」的要求。
- 同時加上 `pull_request`，讓 PR 也會跑檢查。
- `concurrency` 設定讓同一個 branch 有新 push 時，自動取消還在執行的舊 pipeline，節省 runner 資源。

**(2) 三個必備檢查**

| 步驟                 | 指令                   | 對應 package.json script | 用途             |
| -------------------- | ---------------------- | ------------------------ | ---------------- |
| TypeScript typecheck | `npm run typecheck`    | `tsc --noEmit`           | 檢查 TS 型別錯誤 |
| Prettier check       | `npm run format:check` | `prettier --check .`     | 檢查程式碼格式   |
| Run tests            | `npm test -- ...`      | `vitest run`             | 執行單元測試     |

執行順序為「先 typecheck → 再 Prettier → 最後 test」，採 fail-fast：前面的檢查失敗就不會浪費時間跑後面的步驟。

**(3) 失敗時 pipeline 顯示失敗**

- 每個檢查步驟（`npm run typecheck`、`npm run format:check`、`npm test`）只要 exit code 非 0，GitHub Actions 就會把該 step 標記為失敗，整個 job 與 pipeline 也會變成紅色失敗狀態。
- 因此任一檢查（型別、格式、測試）失敗，pipeline 一定會顯示失敗。
- `Generate test summary` 與 `Upload test report artifact` 使用 `if: always()`，所以即使測試已經失敗，測試結果摘要仍會被產生並顯示，方便除錯（這兩步只負責呈現，不影響成敗判定）。

**(4) 將測試結果顯示在 Actions 結果頁面**

策略：**vitest 內建 reporter + 自寫 script 寫入 Job Summary**，全程只用 GitHub 官方 action。

1. Vitest 內建 `junit` reporter，用 `--reporter=junit --outputFile.junit=reports/vitest-junit.xml` 產生標準 JUnit XML 報告。
2. Vitest 內建 `github-actions` reporter：測試失敗時會在 Actions 結果頁面對應的程式碼行產生 inline 註記（annotation）。
3. 自寫 `scripts/junit-summary.mjs`（純 Node.js，無外部套件）解析 JUnit XML，產生 Markdown 表格（總數 / 通過 / 失敗 / 略過 + 每筆測試明細），並寫入 `$GITHUB_STEP_SUMMARY`，使測試結果直接顯示在該次執行的 **Summary** 頁面。
4. 用 GitHub 官方 `actions/upload-artifact@v4` 把 `reports/` 上傳成 artifact，方便事後下載原始 JUnit XML。
5. 摘要與上傳步驟皆加 `if: always()`，確保**即使測試失敗，測試結果依然會顯示**。

**使用的工具與策略總結**

- `actions/checkout@v5`（官方）：取得原始碼。
- `actions/setup-node@v5`（官方）：安裝 Node 22 並啟用 npm cache 加速。
- `npm ci`：依 `package-lock.json` 安裝，確保環境一致可重現。
- vitest 內建 `junit` / `github-actions` reporter：產生標準報告與失敗註記。
- 自寫 `scripts/junit-summary.mjs`：解析 JUnit XML 並寫入 `$GITHUB_STEP_SUMMARY`，將測試結果視覺化呈現於 Actions 頁面。
- `actions/upload-artifact@v4`（官方）：保存測試報告。

> 設計取捨：原可使用 Marketplace 的 `mikepenz/action-junit-report` 一鍵呈現，但為了不依賴第三方 action（供應鏈與權限考量），改以 vitest 內建能力 + 自寫 script 達成相同效果，並移除原本第三方 action 才需要的 `checks: write` 權限，最小化 workflow 權限。

---

## 二、CI 執行結果截圖（成功）

> 請貼上至少一張「成功執行」的截圖，包含：
>
> - GitHub Actions 頁面，pipeline 整體為綠色勾勾
> - 該次執行的 **Summary** 頁面中由 `Generate test summary` 產生的測試表格（所有測試 pass）

（截圖貼此）

---

## 三、失敗案例說明

> 請故意製造一個錯誤，下列三種擇一即可，並附上 pipeline failed 的截圖。

### 範例做法（擇一）

**A. TypeScript 型別錯誤**

在 `src/app.ts` 加入一行型別錯誤，例如：

```ts
const x: number = 'this is a string'; // 型別錯誤
```

→ `TypeScript typecheck` 步驟會失敗。

**B. Prettier 格式錯誤**

把 `src/app.ts` 某行的縮排或引號改成不符合 Prettier 規則（例如多加空白）。

→ `Prettier check` 步驟會失敗。

**C. 測試失敗**

修改 `test/app.test.ts`，把斷言改成錯誤的期望值：

```ts
expect(response.statusCode).toBe(500); // 實際是 200，會失敗
```

→ `Run tests` 步驟會失敗，`Generate test summary` 仍會在 Summary 頁面顯示 ❌ 的測試結果。

### 截圖與說明

（貼上 pipeline failed 截圖）

- **錯誤原因**：（說明你製造的是哪一種錯誤、為什麼會失敗）
- **修正方式**：（說明如何把錯誤改回正確，pipeline 恢復綠色）

---

## 四、繳交檔名

完成後請將本報告匯出成 PDF，檔名格式：

```
313554006_姓名_CICD_作業.pdf
```
